(() => { const focusable = 'a[href],button:not([disabled]),[tabindex]:not([tabindex="-1"])'; document.addEventListener('DOMContentLoaded', () => { const header = document.querySelector('[data-paw-header]'); if (header) { const updateHeader = () => header.classList.toggle('is-scrolled', window.scrollY > 10); updateHeader(); window.addEventListener('scroll', updateHeader, { passive: true }); }
document.querySelectorAll('announcement-bar').forEach((bar) => { const items = [...bar.querySelectorAll('[data-announcement-item]')]; let current = 0; let timer; const show = (index) => { current = (index + items.length) % items.length; items.forEach((item, i) => { item.hidden = i !== current; item.classList.toggle('is-active', i === current); }); }; const start = () => { if (bar.dataset.rotate === 'true' && items.length > 1) timer = setInterval(() => show(current + 1), Number(bar.dataset.interval) || 5000); }; bar.querySelector('[data-announcement-next]')?.addEventListener('click', () => show(current + 1)); bar.querySelector('[data-announcement-previous]')?.addEventListener('click', () => show(current - 1)); bar.addEventListener('mouseenter', () => clearInterval(timer)); bar.addEventListener('mouseleave', start); bar.querySelector('[data-announcement-dismiss]')?.addEventListener('click', () => { bar.hidden = true; sessionStorage.setItem('paw-announcement-dismissed', 'true'); }); if (sessionStorage.getItem('paw-announcement-dismissed') === 'true') bar.hidden = true; start(); });
const drawer = document.querySelector('[data-mobile-drawer]'); const opener = document.querySelector('[data-drawer-open]'); let lastFocus; const closeDrawer = () => { if (!drawer) return; drawer.hidden = true; drawer.setAttribute('aria-hidden', 'true'); document.body.classList.remove('paw-drawer-open'); opener?.setAttribute('aria-expanded', 'false'); lastFocus?.focus(); }; const openDrawer = () => { if (!drawer) return; lastFocus = document.activeElement; drawer.hidden = false; drawer.setAttribute('aria-hidden', 'false'); document.body.classList.add('paw-drawer-open'); opener?.setAttribute('aria-expanded', 'true'); drawer.querySelector('.paw-drawer__panel').focus(); }; opener?.addEventListener('click', openDrawer); drawer?.querySelectorAll('[data-drawer-close]').forEach((button) => button.addEventListener('click', closeDrawer)); document.addEventListener('keydown', (event) => { if (event.key === 'Escape') { closeDrawer(); document.querySelectorAll('.paw-nav__toggle[aria-expanded="true"]').forEach((button) => button.click()); } if (event.key === 'Tab' && drawer && !drawer.hidden) { const nodes = [...drawer.querySelectorAll(focusable)]; if (!nodes.length) return; if (event.shiftKey && document.activeElement === nodes[0]) { event.preventDefault(); nodes.at(-1).focus(); } else if (!event.shiftKey && document.activeElement === nodes.at(-1)) { event.preventDefault(); nodes[0].focus(); } } });
document.querySelectorAll('.paw-nav__toggle').forEach((button) => button.addEventListener('click', () => { const menu = document.getElementById(button.getAttribute('aria-controls')); const open = button.getAttribute('aria-expanded') === 'true'; document.querySelectorAll('.paw-nav__toggle').forEach((other) => { other.setAttribute('aria-expanded', 'false'); document.getElementById(other.getAttribute('aria-controls')).hidden = true; }); button.setAttribute('aria-expanded', String(!open)); menu.hidden = open; })); }); })();
